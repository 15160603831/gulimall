<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hwj.mall.product.dao.PmsSkuSaleAttrValueDao">

    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.hwj.mall.product.entity.PmsSkuSaleAttrValueEntity" id="pmsSkuSaleAttrValueMap">
        <result property="id" column="id"/>
        <result property="skuId" column="sku_id"/>
        <result property="attrId" column="attr_id"/>
        <result property="attrName" column="attr_name"/>
        <result property="attrValue" column="attr_value"/>
        <result property="attrSort" column="attr_sort"/>
        <result property="deleteFlag" column="deleteFlag"/>
    </resultMap>
    <select id="getSaleAttrBySpuId" resultType="com.hwj.mall.product.vo.SkuItemSaleAttrVo">
        select ssav.attr_id attrId,ssav.attr_name attrName,group_concat(DISTINCT ssav.attr_value) attrValues
        from pms_sku_info si
                 left join pms_sku_sale_attr_value ssav on
            ssav.sku_id = si.sku_id
        where spu_id=30
        group by ssav.attr_id,ssav.attr_name
    </select>


</mapper>